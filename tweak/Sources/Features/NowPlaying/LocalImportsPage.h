#import <UIKit/UIKit.h>
// A nil source opens the personal import library. A source with a target starts
// an explicitly selected repair; target refers to existing download history.
UIViewController *SGLocalImportsPageCreate(NSURL *source, NSString *title, NSString *artist, NSDictionary *target);
